import type { Meta, StoryObj } from '@storybook/angular';
import { RelatedNotesFooterComponent } from './related-notes-footer.component';

const meta: Meta<RelatedNotesFooterComponent> = {
  component: RelatedNotesFooterComponent,
  title: 'Opportunity DD / Related Notes / Related Note Foooter',
};
export default meta;
type Story = StoryObj<RelatedNotesFooterComponent>;

export const Primary: Story = {
  args: {},
};
